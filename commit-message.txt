feat: add automatic GitHub release checks for installed Windows builds

- expose update status, download progress, and restart-to-install controls
- keep portable builds on an explicit manual-update path
- publish NSIS update metadata and blockmaps from the release workflow