feat: add persistent episode downloads

- add a serialized download queue with progress, cancellation, and retry support
- download the active episode server and quality through bundled FFmpeg
- add a Downloads tab and configurable destination folder
- persist job history and recover interrupted downloads after restart
- secure download operations behind validated, typed Electron IPC
- package FFmpeg licenses and third-party notices with Windows and Linux builds
- add tests for filenames, collisions, queue order, recovery, headers, and progress
