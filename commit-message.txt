feat: add ad blocking presets

- move embedded-player filtering into an Electron adblock service
- add Off, EasyList only, Basic, Balanced, and Strict filter presets in Settings
- support uBlock/uAssets list combinations with cached network-only engines
- keep AniPlay's lightweight known ad-host blocker configurable
- persist and display session and all-time blocked request counters
- default new installs to the safer EasyList-only preset
