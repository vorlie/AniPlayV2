feat: add AniList account and discovery integration

- move AniList requests into a cached and rate-aware Electron service
- add secure browser OAuth login and encrypted session persistence
- build a personalized dashboard with lists, recommendations, airing, and seasonal data
- add anime detail pages with metadata and explicit list management
- match AniList entries to playback results with confidence scoring and saved corrections
- normalize AniList descriptions and improve banner presentation
- expose validated AniList operations through typed IPC
- add tests for media normalization, descriptions, and catalog matching
