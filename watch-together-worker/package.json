{
  "name": "watch-together-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types --check && tsc --noEmit && wrangler deploy --dry-run",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@types/node": "^24.13.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.112.0"
  },
  "dependencies": {
    "@emnapi/core": "^1.11.2",
    "@emnapi/runtime": "^1.11.2"
  }
}
