# AniPlay 1.13.1

## Reliability

- Added read-only AllAnime runtime crypto diagnostics for inspecting the active epoch, build, bootstrap parts, derived key, query metadata, cache lifetime, and fallback reason, with a portable JSON export for compatible projects.

## Verification

- Passed the production UI build and scoped lint checks.
